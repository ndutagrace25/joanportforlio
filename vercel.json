{
  "version": 2,
  "builds": [
    {
      "src": "src",
      "use": "@vercel/node",
      "config": {
        "commands": {
          "build": "npm config set legacy-peer-deps true && npm run build"
        }
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
